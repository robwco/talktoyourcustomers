<%= form_for(@email) do |f| %>
  <% if @email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@email.errors.count, "error") %> prohibited this email from being saved:</h2>

      <ul>
      <% @email.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subject %><br>
    <%= f.text_field :subject, :placeholder => "Title your email" %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message, :placeholder => "Enter your message" %>
  </div>
  <div class="actions">
    <%= f.submit "Talk to Your Customers"%>
  </div>
	<p>Don't know what to say? <a href="#">Here are some great examples</a></p>
<% end %>
